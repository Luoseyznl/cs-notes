\documentclass[border=15pt]{standalone} % 注意这里：去掉了 tikz 选项
\usepackage{ctex}      % 提供中文支持
\usepackage{array}
\usepackage{tabularx}
\usepackage{booktabs}  % 优雅的三线表
\usepackage{xcolor}
\usepackage{colortbl}

% 保持与文件系统表格完全一致的深灰蓝表头色
\definecolor{headerbg}{RGB}{52, 73, 94} 
\definecolor{headertext}{RGB}{255, 255, 255} 

\begin{document}

% 设置无衬线字体
\sffamily
% 行高设为 1.5，保持舒适的阅读留白
\renewcommand{\arraystretch}{1.5}

% 总宽度设为 16cm，第一列加粗占 2.5cm，第二列占 4cm，第三列自动铺满
\begin{tabularx}{16cm}{>{\bfseries}p{2.5cm} p{4cm} X}
	\rowcolor{headerbg}
	\toprule
	\textcolor{headertext}{\textbf{Shell}} & \textcolor{headertext}{\textbf{全称/溯源}} & \textcolor{headertext}{\textbf{核心特点与应用场景}}                     \\
	\midrule

	bash                                   & Bourne again shell                     & GNU 项目开发，Unix Bourne shell 的开源替代品。兼容性高，为现代主流 Linux 发行版的默认终端环境。 \\
	\addlinespace[0.5ex]

	zsh                                    & Z shell                                & 结合 bash、tcsh 与 korn 特性。支持高级编程、共享历史记录与丰富的提示符主题定制。               \\
	\addlinespace[0.5ex]

	ash                                    & Almquist shell                         & 轻量级 shell，系统资源开销小，且向下兼容 bash。常用于路由器、嵌入式等资源受限的系统环境。             \\
	\addlinespace[0.5ex]

	korn (ksh)                             & KornShell                              & 兼容早期 Bourne shell，引入关联数组与浮点运算等高级编程特性。多见于早期的商业 Unix 系统。         \\
	\addlinespace[0.5ex]

	tcsh                                   & TENEX C shell                          & 核心特征是将 C 语言的语法元素引入 shell 脚本，适合具备 C 语言背景的开发者及特定工程环境使用。          \\

	\bottomrule
\end{tabularx}

\end{document}